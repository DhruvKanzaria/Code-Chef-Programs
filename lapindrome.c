#include <stdio.h>
#include <string.h>

int main(void)
{
    int T;
    scanf("%d",&T);
    for(int i=0;i<T;i++)
    {
        char s[1000];
        int n;
        scanf("%s",&s);
        n=strlen(s);
        if (n/2==0)
        {
            for(int i=0;i<n/2;i++)
            {
                
            }
        }
    }
    return 0;
}